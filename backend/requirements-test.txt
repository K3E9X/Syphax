# Dependency set to IMPORT and RUN the unit tests in CI. Mostly pure logic, so
# the heavy proxy/server stack (uvicorn, mitmproxy, psycopg) stays out and CI
# stays fast - but fastapi IS needed: the API layer holds the authorization
# gates, and while it was absent the one API test skipped in silence.
# The full production requirements are still resolution-checked in CI
# (pip install --dry-run -r requirements.txt) to catch version conflicts.
asyncpg==0.31.0
httpx[socks]==0.28.1
pydantic==2.13.5
pydantic-settings==2.6.1
python-dotenv==1.2.2
dnspython==2.7.0
arq==0.28.0
cryptography==50.0.1
# The sandbox isolation tests assert against docker-compose.yml: that config
# IS the isolation (no route to Postgres, no env_file), so CI has to parse it.
fastapi==0.141.1
PyYAML==6.0.2
pytest==9.0.3
pytest-asyncio==1.4.0
# Linting runs in the same job as the tests; see backend/ruff.toml for
# which rules are on and why the noisy ones are not.
ruff==0.16.8
