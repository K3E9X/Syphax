fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.6.1
# [socks] pulls socksio. Without it every socks5:// proxy (the documented Tor
# setup) raised on client construction, and the error was swallowed into a
# misleading "public IP unreadable".
httpx[socks]==0.28.1
python-dotenv==1.0.1
mitmproxy==11.0.2
asyncpg==0.31.0
psycopg[binary]==3.3.5
arq==0.28.0
dnspython==2.7.0
# Capped at <44.1 by mitmproxy 11.0.2, which shares this environment with the
# API (entrypoint.sh runs mitmdump and uvicorn side by side). Moving to a
# current cryptography means giving mitmproxy its own container first.
cryptography==44.0.3
